# `assemble_tools/high_tandem_repeat` — tandem-repeat k-mer profile

| script | role |
|--------|------|
| `count_kmer_distr.pl` | per-chromosome k-mer distribution, to flag high-tandem-repeat regions (`-out_dir`) |

```sh
perl count_kmer_distr.pl in_sampleID.chr.fa -out_dir out_sampleID_dir/
```
_Hand-written._
